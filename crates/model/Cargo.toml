[package]
name = "model"
version = "0.1.0"
edition = "2024"

[dependencies]
glam = "0.33.3"
hecs = "0.11.1"
log = { version = "0.4", features = ["kv"] }
physics = { path = "../physics" }

[dependencies.derive_more]
version = "2.1.1"
features = ["from", "into"]
